// El enumerado DocType contiene los tipos de documento soportados por el sistema.
// Los tipos de documento soportados son: CC, CE, NIT y PASSPORT.

export enum DocType {
  CC = 'CC',
  CE = 'CE',
  NIT = 'NIT',
  PASSPORT = 'PASSPORT',
}
