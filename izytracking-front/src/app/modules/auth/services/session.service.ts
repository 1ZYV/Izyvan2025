import { Injectable } from '@angular/core';

/**
 * Servicio para gestionar la sesión del usuario.
 * Puede almacenar y recuperar información de sesión.
 */
@Injectable({
  providedIn: 'root'
})
export class SessionService {

  constructor() { }
  // Métodos para gestionar la sesión pueden agregarse aquí
}
